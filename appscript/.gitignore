# clasp local credentials (never commit)
.clasprc.json

# Generated by build.js from ../dashboard-multi-issue.html
# Index.html is checked in so the project is reproducible without a build step,
# but during local iteration build.js may regenerate it. Uncomment the next
# line if you prefer to never commit generated HTML.
# Index.html
